import React from "react";
import { useQuery } from "@apollo/client";
import { useMutation } from "@apollo/client";



const ModelCard = () => {
  return (
    <div className="mt-3 mb-3 p-3 bg-light row">
      {/* use map function to show all  */}
    </div>
  );
};

export default ModelCard;